# Copy this file to terraform.tfvars and fill in your actual values
# DO NOT commit terraform.tfvars to version control
# Sensitive values can be set via environment variables (TF_VAR_<name>) or tools like Vault

proxmox_api_url          = "https://proxmox.lab.home/api2/json"
proxmox_api_token        = "terraform@pve!provider=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
proxmox_tls_insecure     = false  # Set to true for self-signed certificates
proxmox_nodes = {
  openwebui = "pve1",
  searxng   = "pve1",
}
proxmox_host_default_pwd = "changeme"
ollama_host              = "http://ollama.lab.home:11434"
template_file_id         = "pve-cluster:vztmpl/debian13-docker-template.tar.gz"
static_ips = {
  searxng    = "192.168.1.30",
  open_webui = "192.168.1.40",
}
# Optional: Proxmox bridge for the container NICs.
# Defaults to "vmbr0" (flat network). Override for an isolated VLAN/lab bridge, e.g. "vmbr1".
# network_bridge         = "vmbr0"
# network_interface_name = "eth0"   # guest NIC name, defaults to eth0
enable_replication           = false  # Set to true for multi-node clusters
replication_target           = ""     # e.g. "pve2"
openwebui_memory_dedicated   = 1536
openwebui_memory_swap        = 768
searxng_memory_dedicated     = 512
searxng_memory_swap          = 256
openwebui_disk_size      = 50
searxng_disk_size        = 30
